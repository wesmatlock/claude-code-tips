**REQUIRED:** You MUST load MCP tools via this tool BEFORE calling them directly.

**Query modes:**
1. **Direct selection** - \`select:mcp__slack__read_channel\` when you know the tool
2. **Keyword search** - "slack message" to find matching tools

<example>
User: List files in src
Assistant: [Calls MCPSearch with query: "select:mcp__filesystem__list_directory"]
[Then calls the MCP tool]
</example>

NEVER call MCP tools without loading them first.
